import React from 'react';

const CheckDisease = () => {
    return (
        <div>
            <h1>Check Disease</h1>
            <p>Upload an image to check for plant diseases.</p>
        </div>
    );
};

export default CheckDisease;